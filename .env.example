# Kaggle API credentials
# You can set these environment variables instead of using ~/.kaggle/kaggle.json
KAGGLE_USERNAME=your_kaggle_username
KAGGLE_KEY=your_kaggle_api_key

# Optional: Proxy configuration
# KAGGLE_PROXY=http://proxy.example.com:8080

# Optional: Default download path
# KAGGLE_PATH=/path/to/downloads

# Logging level (trace, debug, info, warn, error)
RUST_LOG=info